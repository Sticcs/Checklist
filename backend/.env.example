# Copy this file to .env and fill in a real value before running the server.
# Must stay fixed across restarts - changing it invalidates every logged-in
# session's cookie.
SECRET_KEY=change-me-to-a-long-random-value
